import Foundation

// Enums is the same as Struct except we know all cases at runtime

struct carModel {
    let brand: String
    let model: String
}

struct CarBrand {
    let title: String
}

// var car1: carModel = carModel(brand: "Ford", model: "Mustang")
// var car2: carModel = carModel(brand: "Ford", model: "Alto")
// var car3: carModel = carModel(brand: "Mahindra", model: "XUV700")

var car1: CarModel 